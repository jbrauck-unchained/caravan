import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontSize: 12,
  },
  palette: {
    primary: {
      main: "#8600e0",
    },
    secondary: {
      main: "#e0e0e0",
    },
  },
});

export default theme;
